name: Build

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary publication paused while branch writers settle.'
